# tasks file for debian-mt
- name: Installing Prometheus
  apt:
    name: prometheus2
    state: latest
    update_cache: yes

- name: Starting Prometheus
  service:
    name: Prometheus
    state: started
